::compiler: "VSharp BuiltIn Compiler"
::version: str(ver/*float4*/ 2.0.1.4)
:/:
using float4 as ver execute referance(svar version) run vsharp.init(vsharp.modes.GIT)
if(referance(float4/ver) == vsharp.modes.GIT) {
	2.01.4;
}
